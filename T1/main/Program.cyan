package main

object Program

    func run {
        var test = Json new;
        assert test getDefaultIdent == 4;

        var p = Program();
        var j = Json();
        var t = j toJson: p;
    }

    //func getSomething1 -> String = "bca";

    func getStrings -> Array<String> {
        return [ "a", "bbb", "cccccc" ];
    }

    func getSomething3 -> Double = 5.756;

end
